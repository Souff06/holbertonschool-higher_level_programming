#include "sort.h"
#include <stdio.h>
#include <stdlib.h>
#include <time.h>


void selection_sort(int *array, size_t size)
{
    size_t i, j, k;
    int temp;

    if (array == NULL || size == 0)
        return;
    for (i = 0; i < size - 1; i++)
    {
        for (j = 0; j)
    }
}